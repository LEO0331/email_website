{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "24.x",
    "npm": "11.x"
  },
  "scripts": {
    "start": "node index.js",
    "server": "nodemon index.js",
    "client": "npm run start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "vercel-build": "npm install --prefix client --legacy-peer-deps && npm run build --prefix client",
    "test": "jest --runInBand"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "resend": "^3.0.0",
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "jest": "26.6.0"
  }
}
